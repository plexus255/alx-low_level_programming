#include "main.h"

/**
 * mul - take input
 * Description:a function that multiple two integer.
 * @a: first parameter format
 * @b: second parameter format
 * Return: product
 */

int mul(int a, int b)
{
	int product;

	product = a * b;
	return (product);
}

