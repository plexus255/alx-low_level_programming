#include "main.h"

/**
 * add - takes no input
 * Description: function that adds two integers and returns the result.
 * @a: parameter format for the func.
 * @b: parameter format
 * Return: the computed addition of the two parameter formats
 */

int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}

